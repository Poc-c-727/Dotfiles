# Super
# CTRL
# ALT
# Space
# Tab

# trigger when the switch is toggled
#bindl=,switch:off:lid,exec,hyprlock
# trigger when the switch is turning on
#bindl=,switch:on:lid,exec,hyprctl keyword monitor "eDP-1, disable"
# trigger when the switch is turning off
#bindl=,switch:off:lid,exec,hyprctl keyword monitor "eDP-1, 1366x768@60.06Hz , 0x0, 1"


# Flags dos binds: https://wiki.hypr.land/Configuring/Binds/#bind-flags

# See https://wiki.hypr.land/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Lockscreen
bind = $mainMod SHIFT CTRL, L, exec, hyprlock 

# Screen brightness
bind = , XF86MonBrightnessUp, exec, brightnessctl s +5%
bind = , XF86MonBrightnessDown, exec, brightnessctl s 5%-

# Example binds, see https://wiki.hypr.land/Configuring/Binds/ for more
bind = $mainMod, return, exec, $terminal
bind = $mainMod, C, killactive,
bind = $mainMod, M, exit,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, Space, togglefloating,
bind = $mainMod, D, exec, $menu
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, V, togglesplit, # dwindle
bind = $mainMod, O, exec, $Toggle-Status-Bar
bind = $mainMod, N, exec, org.gnome.gitlab.cheywood.Buffer
bind = $mainMod, B, exec, $terminal bluetui

# Foco da Janela 
    # Setas
        bind = $mainMod, left, movefocus, l
        bind = $mainMod, right, movefocus, r
        bind = $mainMod, up, movefocus, u
        bind = $mainMod, down, movefocus, d
    # Homerow
        bind = $mainMod, H, movefocus, l
        bind = $mainMod, L, movefocus, r
        bind = $mainMod, K, movefocus, u
        bind = $mainMod, J, movefocus, d
    # FullScreen
        bind = $mainMod, F, fullscreen, 1

# Mudar posiçãoo da janela
    bind = $mainMod ALT, H, movewindow, l
    bind = $mainMod ALT, L, movewindow, r
    bind = $mainMod ALT, K, movewindow, u
    bind = $mainMod ALT, J, movewindow, d

# Resize
    bind = $mainMod, R, submap, resize
    submap = resize
    binde = , H, resizeactive, -10 0
    binde = , L, resizeactive, 10 0
    binde = , K, resizeactive, 0 -10
    binde = , J, resizeactive, 0 10

    bind = CTRL, H, movefocus, l
    bind = CTRL, L, movefocus, r
    bind = CTRL, K, movefocus, u
    bind = CTRL, J, movefocus, d


    bind = SHIFT, H, movewindow, l
    bind = SHIFT, L, movewindow, r
    bind = SHIFT, K, movewindow, u
    bind = SHIFT, J, movewindow, d

    bind = $mainMod, R, submap, reset
    bind = , escape, submap, reset
    submap = reset



# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic
bind = $mainMod, S, exec, $Toggle-Status-Bar
bind = $mainMod SHIFT, S, exec, $Toggle-Status-Bar


# Percorrer os workspaces existentes | https://wiki.hypr.land/Configuring/Dispatchers/#workspaces
    # Com Mouse
        bind = $mainMod, mouse_down, workspace, e-1
        bind = $mainMod, mouse_up, workspace, e+1

    # Com Teclado
        bind = $mainMod CTRL, H, workspace, r-1    
        bind = $mainMod CTRL, L, workspace, r+1

        bind = $mainMod CTRL, left, workspace, e-1
        bind = $mainMod CTRL, right, workspace, e+1
        

# Mudar Janela de Workspace
    # Com Mouse
        bind = $mainMod SHIFT, mouse_down, movetoworkspace, e-1

    # Com Teclado
        bind = $mainMod SHIFT, L, movetoworkspace, r+1
        bind = $mainMod SHIFT, H, movetoworkspace, r-1
    

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

bindl = $mainMod CTRL, Space, exec, playerctl play-pause
bindl = $mainMod CTRL, N, exec, playerctl next
bindl = $mainMod CTRL, B, exec, playerctl previous